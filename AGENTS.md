# 项目目标与不可偏移约束

## 核心目标

本项目要做的是一套 **AI 指挥角色完成连续、流畅表演的动态漫生产系统**，不是立绘播放器，也不是依靠卡片人物平移、缩放、震屏和特效伪装动作。

用户应能用自然语言描述表演，例如：

> 角色迟疑半秒，回头看门口，后退一步，突然拔刀前冲，被长枪格挡后失去平衡。

系统需要把它转成同一角色可重复执行、可编辑的连续动作，并保持角色身份、服装、武器、空间位置和剧情状态一致。

《阳寿伏妖》仅作为动态漫表现力与叙事节奏的参考，不复用其人物、剧情或画面资产。

## 主技术路线

主路线固定为：

**剧本 → AI Director → 结构化动作计划 → 骨骼动画执行 → 动作融合与 IK 修正 → 摄像机/VFX/声音 → 卡通化成片**

实现必须包含或为以下能力预留清晰接口：

- 带骨骼的角色资产和可复用动作数据。
- 动作检索、动作生成或视频动作捕捉，缺失动作可补充到动作库。
- 动画重定向、动作融合、Root Motion、脚底锁定和手部/武器 IK。
- 双人互动中的目标、距离、接触点和时序约束。
- 视线、表情、口型、呼吸和细小身体动作。
- AI 只表达导演意图、动作节拍和约束，不直接逐帧生成骨骼坐标。
- Unreal Engine 或 Blender 等骨骼动画系统承担最终角色动作执行；Web/Pixi 层只适合作为导演台、时间线、预览器或合成界面。

纯生成视频只能用于幻境、变身、转场、特殊特写等局部镜头，不能作为长篇连续剧情的主要角色执行层。

## 现有实现的定位

当前 PixiJS 版本只是早期的镜头时间线与合成验证，不是最终动画引擎。

可以保留：

- AI Director 与 Shot DSL 的方向。
- 剧本拆镜、时间线、摄像机、VFX、声音、预览和导出能力。
- 角色、服装、武器、伤势、场景等连续性状态。

必须替换或重做：

- 静态 PNG 角色作为主演员执行层。
- 以横移、旋转、缩放和残影代替真实肢体动作的实现。
- 只记录 `dash_forward` 等标签、没有连续运动数据和约束的 Actor DSL。

不要继续为静态卡片人物扩充动作功能。任何新功能应优先证明骨骼角色动作链路，而不是美化现有立绘演示。

## 第一阶段验收标准

验收样片必须让固定角色连续完成：

**走入场景 → 环顾 → 说话 → 拔刀 → 转身闪避 → 与对手兵器接触 → 受击踉跄 → 恢复架势**

同时满足：

- 动作由自然语言或结构化导演指令触发，不是为样片逐帧硬编码。
- 动作之间有自然过渡，人物不能滑步、瞬移或突然回到静态立绘。
- 同一角色全程不换脸、不换服装、不改变武器结构。
- 脚底接地，手能握住武器，兵器接触时双方时序和位置基本可信。
- 角色朝向由移动方向和目标决定；行走时朝速度方向，停下和交互时朝目标，不能始终正对镜头。
- 动作、镜头和节奏可重新编辑、重新执行；一次性生成的视频不算完成闭环。

以下结果一律不能作为“动作系统完成”的证据：

- 两张透明人物图的横向移动或碰撞。
- 主要依靠闪白、震屏、速度线和剪辑掩盖没有肢体动作。
- 用频繁切镜回避连续动作和空间关系。
- 只展示单个预制动作，不能按 AI 指令组合成一段表演。

## 关键工程判断

- 流畅度主要来自骨骼动画、动作数据、融合、IK 和接触修正，不来自 LLM 本身。
- 单人通用动作可以优先自动化；双人打斗、摔抱和精确武器碰撞必须允许引用双人动作、真人参考视频或人工修正。
- 当功能取舍与上述目标冲突时，以“AI 可控的连续角色表演”优先，并明确报告偏离，不得静默降级为立绘运动。
